# Environment — local files only, never commit real values
.env
.env.example
.env.local
.env.*.local
.env.development

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Planning artifacts (local only, not for source control)
plans/

# Claude Code local settings (machine-specific, not shared)
.claude/settings.local.json

.claude/session-data/
.DS_Store
# Audio files — served via Docker volume / Nginx in production
media/sounds/
static/